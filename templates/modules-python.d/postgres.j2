local:
    name     : 'postgres-local'
    database : 'postgres'
    user     : 'netdata'
    password : '{{lookup('file', '/tmp/psqlpass') }}'
    host     : 'localhost'
    port     : 5432
    retries: {{ plugin_retries }}
    autodetection_retry: {{ plugin_autodetection_retry }}